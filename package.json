{
  "name": "footprints",
  "version": "1.0.0",
  "description": "Footprints of previous requests queried via a GET request to /footprints endpoint.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "minimist": "^1.2.0"
  }
}
